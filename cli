#!/bin/sh

COMMAND=${1-"help"}

# if ! docker image inspect sw-builder > /dev/null 2>&1; then
  
# fi

docker build --tag="sw-builder" --progress="quiet" .
docker run --network="host" --volume="$(pwd):/app" --rm sw-builder "bin/$COMMAND"